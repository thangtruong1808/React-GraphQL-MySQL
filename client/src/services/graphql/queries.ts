/**
 * GraphQL Queries Module
 * Re-exports all GraphQL queries and mutations for backward compatibility
 * 
 * @deprecated This file is maintained for backward compatibility.
 * New code should import directly from './queries/' submodules.
 */

// Re-export everything from the new structure
export * from './queries/index';
