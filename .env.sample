# Database Configuration
DB_NAME=""
DB_USER=""
DB_HOST=""
DB_PASSWORD=""
DB_PORT=""

# JWT Configuration (Required)
JWT_SECRET=""

#Server Variables:
ACCESS_TOKEN_EXPIRY=""                    # Access token lifetime
REFRESH_TOKEN_EXPIRY=""                   # Refresh token lifetime
REFRESH_TOKEN_EXPIRY_MS=""            # Refresh token expiry in milliseconds
ACTIVITY_CHECK_INTERVAL=""              # Activity check interval
INACTIVITY_THRESHOLD=""                # Inactivity timeout
TOKEN_REFRESH_THRESHOLD=""             # Token refresh threshold
ACTIVITY_THROTTLE_DELAY=""              # Activity throttle delay
MAX_REFRESH_TOKENS_PER_USER= ""           # Maximum refresh tokens per user (increased for multiple sessions)

#Client Variables:
VITE_REQUEST_TIMEOUT=""                # API request timeout
VITE_MAX_RETRIES=""                        # Maximum API retries
VITE_RETRY_DELAY=""                     # Retry delay
VITE_SESSION_DURATION=""             # Session duration
VITE_ACTIVITY_CHECK_INTERVAL=""         # Activity check interval
VITE_INACTIVITY_THRESHOLD=""           # Inactivity timeout
VITE_TOKEN_REFRESH_THRESHOLD=""        # Token refresh threshold
VITE_ACTIVITY_THROTTLE_DELAY=""         # Activity throttle delay
VITE_REFRESH_TOKEN_EXPIRY_MS=""       # Refresh token expiry

# Development and production
SERVER_HOST=localhost

# Optional (with fallbacks)
PORT=4000
NODE_ENV=development

# Client Configuration
VITE_API_URL=http://localhost:4000/graphql


## 🔧 API Endpoints

### GraphQL Endpoint
- **URL**: `http://localhost:4000/graphql`
- **Playground**: `http://localhost:4000/graphql` (in development)

# Development
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Production
#CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Multiple environments
#CORS_ORIGINS=https://app.yourdomain.com,https://admin.yourdomain.com